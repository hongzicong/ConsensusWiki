# Protocol Catalog

| Protocol | Family | Fault model | Timing model | Leader role | Fast path? | Quorum style | Conflict handling | Recovery style | Main metadata | Primary paper |
|---|---|---|---|---|---|---|---|---|---|---|
| [[FastPaxos]] | Paxos / fast consensus | Non-Byzantine | Async safety; eventual good set for progress | Coordinator per round | Yes | Classic and fast quorums with triple-intersection condition | Colliding proposals trigger recovery | Higher round selects a safe value from lower-round evidence | rounds, accepted values, `any` | [[FastPaxos-2006]] |
| [[GPaxos]] | Paxos / generalized fast consensus | Non-Byzantine crash/stop | Async safety; eventual leader/live quorum for classic progress; collision observation for fast progress | Leader per ballot; fast ballots let proposers send directly to acceptors | Yes | Ballot-dependent quorums; fast ballots require triple intersection with any recovery quorum | Non-interfering commands remain compatible; interfering commands collide and are ordered in recovery | Higher ballot uses `ProvedSafe(Q, m, beta)` to select a safe c-struct | c-struct, balnum, `mbal`, `bal`, `val`, lub/glb | [[Generalized-Paxos-2005]] |
| [[EPaxos]] | Paxos / leaderless SMR | Non-Byzantine | Async safety; majority availability for progress | Any replica can be command leader | Yes; tail-sensitive under conflicts | Majority plus fast quorums over `N = 2F + 1` | Interfering commands acquire dependencies; conflict rate is workload/topology sensitive | Prepare / TryPreAccept; recovery is subtle and later work reports bugs/ambiguity | instance, `seq`, `deps`, status | [[EPaxos-2013]], [[EPaxos-Revisited-2021]] |
| [[EPaxosStar]] | Paxos / leaderless SMR | Non-Byzantine crash | Partial synchrony; `e`-faulty synchronous fast guarantee | Any replica can be initial coordinator; per-command recovery coordinator | Yes | Slow/recovery quorums `n - f`; fast quorums `n - e`; optimized bound `n >= max{2e + f - 1, 2f + 1}` | Conflicting commands must be visible in at least one dependency direction | Recover / Validate / Waiting; may recover `Nop` and resubmit | `cmd`, `dep`, `initDep`, `bal`, `abal`, phase | [[Making-Democracy-Work-2025]] |
| [[Mencius]] | Paxos / multi-leader SMR | Non-Byzantine crash-recovery | Async safety; FIFO channels and eventual accurate failure detector for liveness | Rotating per-instance coordinator | No Fast Paxos-style fast quorum; low-latency local coordinator path | Paxos quorum `f + 1` with `n = 2f + 1`; owner-authored `SKIP` for `no-op` | Avoids per-instance contention by owner assignment; optional commutativity-based out-of-order commit | Revocation runs higher Paxos-style round; block revocation with `beta` | instance owner, `Ip`, `no-op`, skip promises | [[Mencius-2008]] |
| [[PigPaxos]] | Paxos / Multi-Paxos communication overlay | Non-Byzantine crash | Async Paxos safety; timeout/retry liveness with eventual bounded delays | Stable Paxos leader; randomly rotated relay followers | No Fast Paxos-style fast path; normal Multi-Paxos path through relays | Majority quorum `floor(N/2) + 1`; relay groups are communication overlays | Single leader orders commands; no dependency conflict handling | Ordinary Paxos leader change; failed relay/follower communication retries with new relays | `PigMsgID`, relay group, ballot, slot, unique voter ids | [[PigPaxos-2021]] |
| [[Atlas]] | Paxos / leaderless SMR | Non-Byzantine crash | Async safety; quorum availability for progress | Any replica can be initial command coordinator; any process can recover | Yes | Fast quorum `floor(n/2) + f`; slow quorum `f + 1`; recovery quorum `n - f` | Dependency union; fast path allows non-matching replies if each dependency appears at least `f` times | MRec recovery selects highest accepted proposal, reconstructs fast proposal, or chooses `noOp` | `cmd`, `dep`, fast quorum, `bal`, `abal`, phase | [[Atlas-2020]] |
| [[SwiftPaxos]] | Paxos / dependency SMR | Non-Byzantine | Async safety; eventual stable ballot | Fixed leader per ballot; leader in fast quorums | Yes | Majority slow quorums; fast quorums intersect in majority | Dependency paths, acyclic committed deps | NewLeaderAck plus Sync recover accepted dependency evidence | ballot, dependency paths, phase | [[SwiftPaxos-2024]] |
| [[Pando]] | Geo-storage / Paxos | Non-Byzantine data-site failures | Quorum safety; available Phase 1b/2 quorums for progress | Optional fallback leader/delegate | Read fast path; write latency approximates one round | Phase 1a, Phase 1b, Phase 2 quorums for erasure-coded splits | Proposal-number conflicts; fallback/recovery | Phase 1b reconstructs previously chosen erasure-coded values | proposal number, value id, split, chosen hint | [[Pando-2020]] |
| [[Rabia]] | Randomized leaderless SMR | Fail-stop crashes; `n ≥ 2f + 1` | Async safety; probabilistic termination with non-faulty majority; stable network for performance | None | Yes; 3 message delays when proposals yield uniform binary state | Wait quorums `n - f`; majority `⌊n/2⌋ + 1`; vote threshold `f + 1` | Ambiguous slots may decide `⊥`; requests are retried from PQ | No separate fail-over; Weak-MVC value-locking carries decisions forward | `PQ`, `seq`, `state`, `vote`, phase, common coin | [[Rabia-2021]] |
| [[CURP]] | Primary-backup fast replication | Fail-stop, non-Byzantine | Async safety; 1 RTT when master and all witnesses respond | Strong master executes and orders; witnesses only provide unordered durability | Yes | Primary-backup `f + 1` replicas plus all-`f` witness fast durability; recovery uses one backup plus one selected witness | Non-commuting unsynced operations force sync fallback | Restore from backup, freeze one witness, replay its mutually commutative records, suppress duplicates with RIFL | witness records, RPC ids, `WitnessListVersion`, unsynced set | [[CURP-2019]] |
| [[Hermes]] | Membership-based reliable replication | Crash-stop plus message/link faults; non-Byzantine | Partial synchrony; LSCs for RM leases; logical clocks order writes | Any replica coordinates each update; no fixed data-path leader | Local reads and one-exposed-RTT writes | Read-one/write-all over current leased membership; majority protocol only for RM changes | Per-key `[version, cid]` order; ordinary writes never abort; losing RMWs abort | Replay original timestamp/value through all current live members | state, `[version, cid]`, `RMW_flag`, `mlt`, epoch, live set | [[Hermes-2020]] |
| [[Copilot]] | Paxos / dual-leader dependency SMR | Non-Byzantine crash; `n = 2f + 1` | Async safety; eventual partial synchrony for liveness | Pilot and copilot each own a log and redundantly process every command | Yes | Fast `f + floor((f + 1) / 2)`; regular/recovery `f + 1` | Reject incompatible cross-log dependencies; deterministic priority orders cycles; duplicate commands execute once | Per-entry Prepare/Accept takeover; ambiguous fast evidence inspects the other log | two logs, prefix dependency, `(cliid, cid)`, ballot, progress | [[Copilot-2020]] |
| [[Avicenna]] | Paxos / single-leader SMR | Non-Byzantine crash and one fail-slow replica; `n = 2f + 1` | Async safety; eventual timely majority for liveness | Real leader orders; next leader shadow-processes and takes over on rotation | No Fast Paxos-style path; Multi-Paxos normal latency | Normal acceptors `f + 2`; commit `f + 1`; rotation logs `2` or Armageddon `f + 1` | One real leader per phase; highest-phase accepted value survives merge | Phase rotation merges logs, then new leader sends `LongAccept` | phase, real/shadow logs, status, `IsShadow`, latency pairs | [[Avicenna-2026]] |
| [[Bodega]] | Paxos / lease-based read SMR | Fail-slow/stop; minority failures; odd `n` | Async consensus; bounded clock drift for leases | One write leader; arbitrary per-key read responders | Local-read fast path; no Fast Paxos-style write path | Majority `m` plus all key responders; stable roster needs `m` leases | Same-key read holds behind uncertain write | Higher-ballot roster revokes/expires leases; new leader runs Prepare | `bal`, `ros`, lease sets/timers, `thresh`, `accnote` | [[Bodega-2026]] |
| [[Jetpack]] | Fast-consensus plugin / Paxos-style shim | Non-Byzantine crash; `n = 2f + 1` | Async safety; host view-change liveness | Host proposer set must all promise/order fast commands | Yes; conflict-free client commit in 1 RTT | Fast `f + ceil(f/2) + 1` including all proposers; recovery `f + 1` | Application conflict predicate; conflicts use host path | Same-view recovery set, majority Accept, original-path stability marker | fast view/log, proposer set, recovery set, command pool | [[Jetpack-2026]] |
| [[FPaxos]] | Paxos / flexible quorums | Non-Byzantine process failure and message loss | Async safety; synchrony/stable leader for progress | Stable leader runs common Phase 2; Phase 1 elects/replaces it | No Fast Paxos-style path | Every `Q1` intersects every `Q2`; same-phase intersection unnecessary | Proposal-number competition; later proposer adopts highest accepted value | Ordinary Phase 1 over a valid `Q1` | ballot, promise, accepted ballot/value, quorum families | [[Flexible-Paxos-2016]] |
| [[OmniPaxos]] | Paxos / sequence-consensus SMR | Non-Byzantine fail-recovery servers and partial link partitions | Partial synchrony for liveness; FIFO link sessions | BLE elects a quorum-connected leader; Sequence Paxos synchronizes it | No separate fast path; stable replication is one leader round trip | Majorities for BLE observation, Prepare, and Accept | Single leader serializes commands; synchronization may replace unchosen suffixes | Majority Prepare/AcceptSync; `PrepareReq` after server/link recovery | ballot, `qc`, log, `acceptedRnd`, `decidedIdx`, stop-sign | [[Omni-Paxos-2023]] |
| [[Hydra]] | Network ordering / ordered unreliable groupcast | Non-Byzantine sequencer/link failures and packet loss | Non-retrograde sequencer clocks for safety; loose synchronization affects latency | No consensus leader; multiple active sequencers stamp messages independently | One sequencer pass; not an application commit path | Application-defined quorum per receiver group for reconfiguration | Orders every pair with overlapping destination groups | Configuration service collects per-group quorum evidence and installs final flush | sequencer ID, clock, per-group multi-stamp, configuration | [[Hydra-2023]] |
| [[HydraPaxos]] | Network-ordered Paxos / SMR | Crash failures; `n = 2f + 1` | Hydra timing plus live majority/leader for progress | Hydra orders; replica leader executes and participates in commit replies | Yes; one client RTT in stable no-drop case | Majority `f + 1`, including leader | Deterministic execution in Hydra order | Recover missing message or leader-agree on `NO-OP`; Hydra removes failed sequencers | Hydra stamp, operation log, drop state | [[Hydra-2023]] |
| [[WPaxos]] | Paxos / WAN multi-leader | Non-Byzantine crash/recovery and message loss | Async safety; live `Q1` and `Q2` plus contention resolution for progress | One owner per object; all nodes may own different objects | Stable-owner local Phase 2; not Fast Paxos | `\|Q1\| = (f_n + 1)(Z - f_z)`; `\|Q2\| = (l - f_n)(f_z + 1)` | Same-object commands share one log; different objects proceed independently | Higher per-object ballot gathers `Q1` and recovers unfinished slots | per-object ballot, slot, owner set, log, locality | [[WPaxos-2020]] |

